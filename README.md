# Card Collections
Built using a React frontend to fetch a database containing generation one pokemon along with additional information about each. This data is then displayed in a table format styled using Tailwind CSS.
